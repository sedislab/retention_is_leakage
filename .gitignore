# large / generated — never git add these
raw/
raw_data/
datasets/
features/
shadows/
runs/
envs/
logs/
hf/
torch/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/**/*.pyc

# data caches and checkpoints
*.npz
*.npy
*.pt
*.pth

# figures/tables are generated, never hand-edited, but ARE regenerable from CSV —
# still keep them out of git; the CSVs under results/ are the tracked evidence.
# figs/*.pdf
# figs/*.png
# tables/*.tex

# paper build artifacts
paper/**/*.aux
paper/**/*.log
paper/**/*.out
paper/**/*.fls
paper/**/*.fdb_latexmk
paper/**/*.synctex.gz

# keep: results/*.csv, results/*.meta.json, results/RUN_LOG.jsonl, code/, analysis/, figs/, tables/, env/

# ---------------------------------------------------------------------------------------------
# Submission branch: internal project-management, assistant-tooling and working-state files are
# not part of the released artifact. They stay on the author's disk; they are simply not tracked.
# ---------------------------------------------------------------------------------------------
# assistant / agent configuration and instructions
CLAUDE.md
.claude/
agents/
# internal planning docs, session state, superseded snapshots, working notes
RESEARCH_PLAN.md
build/
notes/
archive/
# manuscript sources are submitted separately through the review system
paper/
# raw per-shadow bookkeeping (138k tiny JSON files, regenerable; the .npz payloads are already ignored)
shadows_v2/
shadows_v3/
# local tool caches
.ruff_cache/
